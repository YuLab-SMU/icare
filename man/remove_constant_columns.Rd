% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_internal.R
\name{remove_constant_columns}
\alias{remove_constant_columns}
\title{Remove constant (zero-variance) columns from a data frame or matrix}
\usage{
remove_constant_columns(
  data,
  na.rm = FALSE,
  verbose = TRUE,
  allow_empty = FALSE
)
}
\arguments{
\item{data}{A data frame or matrix.}

\item{na.rm}{Logical. If TRUE, NA values are ignored when checking constancy
(i.e., a column with only one non-NA value and the rest NA is considered constant).
If FALSE (default), NA is treated as a distinct value.}

\item{verbose}{Logical. Print messages about removed columns (default TRUE).}

\item{allow_empty}{Logical. If TRUE, allow returning an empty data frame when
all columns are constant (default FALSE, which raises an error).}
}
\value{
A data frame or matrix (same class as input) with constant columns removed.
}
\description{
Identifies and removes columns that have only one distinct value (including NA).
}
\examples{
df <- data.frame(a = 1:5, b = rep(2, 5), c = c(1, NA, 1, 1, 1))
remove_constant_columns(df)                 # removes column 'b'
remove_constant_columns(df, na.rm = TRUE)   # also removes column 'c'
remove_constant_columns(df, verbose = FALSE)
}
