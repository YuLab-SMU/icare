% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prognosis_02_framework.R
\name{surv_evaluate_model}
\alias{surv_evaluate_model}
\title{Evaluate Model Performance on a Survival Task}
\usage{
surv_evaluate_model(learner, object, measures = NULL)
}
\arguments{
\item{learner}{A trained \code{\link[mlr3]{Learner}} object.}

\item{object}{A \code{TaskSurv} or \code{PrognosiX} object. The function
extracts the task using \code{surv_extract_task()}.}

\item{measures}{A list of \code{\link[mlr3]{Measure}} objects. If \code{NULL},
automatically selects:
\itemize{
\item \code{surv.cindex} (always included)
\item \code{surv.graf} if learner supports \code{"distr"} predictions
}}
}
\value{
A data frame with one row containing the performance metrics for each
requested measure.
}
\description{
Evaluates a trained survival learner on a given task using specified measures.
The function automatically selects appropriate measures based on the learner's
\code{predict_type}.
}
\examples{
\dontrun{
library(mlr3proba)
library(survival)

data("veteran", package = "survival")
task <- surv_create_surv_task(veteran, "time", "status")
learner <- lrn("surv.coxph")$train(task)

# Auto-select measures
perf <- surv_evaluate_model(learner, task)
print(perf)

# Custom measures
perf_custom <- surv_evaluate_model(
  learner, task,
  measures = list(msr("surv.cindex"), msr("surv.logloss"))
)
}

}
\seealso{
\code{\link{surv_train_and_tune}}, \code{\link{surv_benchmark_learners}}
}
