% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_functions.R
\name{PlotAUCPval}
\alias{PlotAUCPval}
\title{Feature selection plot: AUC vs. –log10(p‑value) (enhanced)}
\usage{
PlotAUCPval(
  deg_df,
  mat_test,
  group_col = "group",
  auc_thresh = 0.55,
  p_thresh = 0.05,
  selected_fill = "#e84118",
  removed_fill = "#7f8fa6",
  arrow = TRUE,
  base_size = 13,
  save_plot = FALSE,
  save_dir = NULL,
  width = 7,
  height = 6,
  format = "pdf"
)
}
\arguments{
\item{deg_df}{Data frame with columns \code{feature}, \code{logFC}, \code{p.adjust} / \code{p_value}.}

\item{mat_test}{Expression matrix containing the grouping column.}

\item{group_col}{Name of the grouping column in \code{mat_test}.}

\item{auc_thresh}{AUC threshold for selection. Default 0.55.}

\item{p_thresh}{P‑value threshold. Default 0.05.}

\item{selected_fill}{Fill colour for selected features.}

\item{removed_fill}{Fill colour for removed features.}

\item{arrow}{Logical. Draw arrows from labels to points.}

\item{base_size}{Base font size.}

\item{save_plot}{Logical. Save the plot? Default \code{FALSE}.}

\item{save_dir}{Output directory. When \code{NULL} and \code{save_plot=TRUE}
the default Stat figure directory is used.}

\item{width, height}{Plot dimensions (inches).}

\item{format}{File format.}
}
\value{
A \code{ggplot} object.
}
\description{
Calculates per‑feature ROC AUC and plots it against –log10(p‑value).
Points that satisfy both \code{auc_thresh} and \code{p_thresh} are highlighted.
}
\examples{
\dontrun{
stat_obj <- stat_var_feature(stat_obj_test)
last_sig <- ExtractLastTestSig(stat_obj)
last_sig$feature <- last_sig$id
PlotAUCPval(last_sig, stat_obj_test@clean.data, group_col = "SWAB", save_plot = FALSE,p_thresh = 0.1, auc_thresh = 0.5)
}
}
