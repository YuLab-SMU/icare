% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prognosis_02_framework.R
\name{get_cf}
\alias{get_cf}
\title{Extract Cutoffs from a Risk Stratification Plot}
\usage{
get_cf(km_plot)
}
\arguments{
\item{km_plot}{A \code{ggsurvplot} object returned by \code{\link{surv_plot_risk_km}}.}
}
\value{
A numeric vector of cutoffs used in the plot, or \code{NULL} if not available.
}
\description{
Retrieves the cutoffs used to create risk groups in a Kaplan-Meier plot
generated by \code{surv_plot_risk_km}.
}
\examples{
\dontrun{
library(mlr3proba)
library(survival)

data("veteran", package = "survival")
task <- surv_create_surv_task(veteran, "time", "status")
learner <- lrn("surv.coxph")$train(task)

p <- surv_plot_risk_km(learner, task, cutoff_method = "median")
cutoffs <- get_cf(p)
print(cutoffs)
}

}
\seealso{
\code{\link{surv_plot_risk_km}}
}
