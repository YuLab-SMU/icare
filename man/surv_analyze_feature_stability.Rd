% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prognosis_02_framework.R
\name{surv_analyze_feature_stability}
\alias{surv_analyze_feature_stability}
\title{Robust Feature Stability Analysis for Sparse Survival Models}
\usage{
surv_analyze_feature_stability(
  object,
  time_col = "time",
  status_col = "status",
  n_repeat = 30,
  train_ratio = 0.8,
  alpha = 1,
  palette_name = "AsteroidCity1",
  seed = 2025,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{PrognosiX} object, data frame, or \code{TaskSurv}.}

\item{time_col}{A character string specifying the name of the time column
(required if \code{object} is a data frame). Default is \code{"time"}.}

\item{status_col}{A character string specifying the name of the status column
(required if \code{object} is a data frame). Default is \code{"status"}.}

\item{n_repeat}{An integer specifying the number of subsampling iterations.
Default is \code{30}.}

\item{train_ratio}{A numeric value specifying the proportion of data to sample
each iteration. Default is \code{0.8}.}

\item{alpha}{Elastic net mixing parameter: 1 = LASSO, 0 = Ridge, 0.5 = elastic net.
Default is \code{1}.}

\item{palette_name}{A character string specifying the name of the Wes Anderson
palette. Default is \code{"AsteroidCity1"}.}

\item{seed}{An integer seed for reproducibility. Default is \code{2025}.}

\item{verbose}{A logical value. Print progress messages. Default is \code{TRUE}.}
}
\value{
A list with four components:
\describe{
\item{stability_index}{Jaccard stability index (mean pairwise Jaccard similarity).}
\item{frequencies}{A data frame of features and their selection frequencies.}
\item{plot}{A \code{ggplot} object of the top 15 features.}
\item{success_rate}{The proportion of successful iterations.}
}
}
\description{
Evaluates the stability of feature selection under repeated subsampling using
\code{glmnet::cv.glmnet} with Cox regression. Provides the Jaccard stability
index and selection frequencies for each feature.
}
\examples{
\dontrun{
library(survival)
data("veteran", package = "survival")

# Stability analysis on data frame
stab <- surv_analyze_feature_stability(
  object = veteran,
  time_col = "time",
  status_col = "status",
  n_repeat = 20,
  alpha = 1
)

print(paste("Stability Index:", round(stab$stability_index, 3)))
print(stab$plot)
}

}
\seealso{
\code{\link{surv_feature_selection_multi}} for multi-method selection
}
