% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyping_06_predict.R
\name{Sub_create_val_object}
\alias{Sub_create_val_object}
\title{Create, Normalise, and Predict Subtypes for a Validation Set}
\usage{
Sub_create_val_object(
  clean.data,
  info.data = data.frame(),
  norm_params,
  train_object,
  methods = c("nmf", "kmeans", "lpa"),
  prefix = "S",
  verbose = TRUE
)
}
\arguments{
\item{clean.data}{Data frame of raw validation features
(samples x variables).}

\item{info.data}{Data frame of clinical/meta variables for the validation
set (optional; same row order as \code{clean.data}).}

\item{norm_params}{Named list produced by \code{Sub_extract_norm_params},
or the legacy \code{list(var = list(min=, max=))} format.
Typically stored at \code{stat_obj@process.info$norm_params}.}

\item{train_object}{Trained \code{Subtyping} object.}

\item{methods}{Character vector of methods to predict; any subset of
\code{c("nmf", "kmeans", "lpa")} (default: all three,
but only methods with a fitted model are actually run).}

\item{prefix}{Subtype label prefix for NMF (default \code{"S"}).}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
A fully populated validation \code{Subtyping} object.
}
\description{
Convenience wrapper that chains:
\enumerate{
  \item \code{CreateSubtypingObject} -- wraps raw validation data.
  \item \code{Sub_apply_norm_params} -- applies training min-max parameters.
  \item \code{Sub_predict_subtypes}  -- predicts subtypes for each requested
    method.
}
}
\examples{
\dontrun{
  # Assuming 'train_obj' is a trained Subtyping object and 'norm_params' extracted
  val_obj <- Sub_create_val_object(
    clean.data = val_clean,
    info.data = val_info,
    norm_params = norm_params,
    train_object = train_obj,
    methods = c("kmeans", "lpa", "nmf")
  )
}
}
