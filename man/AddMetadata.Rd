% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object_utils.R
\name{AddMetadata}
\alias{AddMetadata}
\title{Add or overwrite a column in info.data}
\usage{
AddMetadata(object, col, values)
}
\arguments{
\item{object}{Any of the four object classes.}

\item{col}{Name of the new (or existing) metadata column.}

\item{values}{Vector of values; must match \code{nrow(info.data)} or the
number of samples in the object.}
}
\value{
Object with the updated metadata.
}
\description{
Appends (or replaces) a metadata column in the \code{info.data} slot.
Checks that \code{values} length matches the number of samples.
}
\examples{
\dontrun{
stat_obj <- AddMetadata(stat_obj_test, col = "new_meta", values = 
sample(1:2, nrow(stat_obj_test@clean.data), replace=TRUE))
}
}
