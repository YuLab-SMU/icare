% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_06_deg.R
\name{batch_Wilcoxon_Unified}
\alias{batch_Wilcoxon_Unified}
\title{Unified Interface for DEG Analysis (Binary and Multi-class)}
\usage{
batch_Wilcoxon_Unified(
  mat,
  group_col = "group",
  analysis_type = "auto",
  logfc_mat = NULL,
  logfc_type = "log2ratio",
  p_threshold = 0.05,
  only.pos = NULL,
  save_dir = NULL,
  save_data = FALSE,
  csv_filename = NULL
)
}
\arguments{
\item{mat}{Data frame with numeric features and a group column.}

\item{group_col}{Name of the grouping column.}

\item{analysis_type}{One of:
\itemize{
\item "auto" (default): automatically detect based on number of groups
\item "binary": force binary classification (2 groups)
\item "multiclass": one-vs-rest comparison (2+ groups)
\item "ordered": sequential pairwise comparison (2+ groups with order)
}}

\item{logfc_mat}{Optional data frame for logFC calculation.}

\item{logfc_type}{One of "log2ratio" (default) or "diff".}

\item{p_threshold}{P-value threshold. Default 0.05.}

\item{only.pos}{Logical; filter to positive logFC. Default depends on analysis_type.}

\item{save_dir}{Directory to save results.}

\item{save_data}{Logical; whether to write CSV. Default TRUE.}

\item{csv_filename}{Output filename. If NULL, auto-generated based on analysis_type.}
}
\value{
Data frame with DEG results. For multi-class: includes "target_group" column.
For ordered: includes "comparison" column.
}
\description{
Automatically detects number of groups and routes to appropriate function.
Provides a single entry point for both binary and multi-class DEG analysis.
}
\examples{
\dontrun{
# Binary analysis (2 groups) - automatic
result_binary <- batch_Wilcoxon_Unified(mtcars,group_col = 'vs')
# Multi-class analysis (3+ groups) - automatic
result_multi <- batch_Wilcoxon_Unified(iris,group_col ="Species")
# Ordered analysis (stages)
iris$Species=factor(iris$Species,levels = unique(iris$Species))
result_ordered <- batch_Wilcoxon_Unified(
iris,group_col = "Species",analysis_type = "ordered")
}
}
