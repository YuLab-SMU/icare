% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_functions.R
\name{PlotConfusionMatrix}
\alias{PlotConfusionMatrix}
\title{Plot Confusion Matrix for a Single Model}
\usage{
PlotConfusionMatrix(
  object,
  model_name = NULL,
  test_data = NULL,
  threshold = NULL,
  prob_class = NULL,
  prevalence = NULL,
  palette = c("#d8b365", "#5ab4ac"),
  base_size = 13,
  save_plot = FALSE,
  save_dir = NULL,
  width = 5,
  height = 4.5,
  format = "pdf"
)
}
\arguments{
\item{object}{A Train_Model object with trained models.}

\item{model_name}{Name of the model to plot. If NULL, uses first model.}

\item{test_data}{Test data (optional). If NULL, uses object@split.data$test.}

\item{threshold}{Numeric threshold for probability predictions (0‑1).
If provided, predictions are obtained via type = "prob"
and binarised using this threshold.
If NULL (default), type = "raw" is used.}

\item{prob_class}{Character string indicating which class is considered
positive when using threshold. If NULL, the second level
of the true labels is used as positive class.}

\item{prevalence}{Optional population prevalence (0‑1) of the positive class.
If provided, the function calculates adjusted positive and
negative predictive values using Bayes' theorem.}

\item{palette}{Vector of 2 colors (low, high) for the gradient.}

\item{base_size}{ggplot2 base font size.}

\item{save_plot}{Logical. Save the plot to file?}

\item{save_dir}{Directory to save plot. If NULL, auto-detects.}

\item{width}{Plot width in inches.}

\item{height}{Plot height in inches.}

\item{format}{File format ("pdf", "png", "jpg").}
}
\value{
A ggplot2 object showing the confusion matrix.
}
\description{
Generates a confusion matrix heatmap for a trained model.
Supports both direct class predictions (type = "raw") and probability-based
predictions with user‑defined threshold. Additionally calculates positive and
negative predictive values, optionally adjusted for a given population prevalence.
}
\examples{
\dontrun{
model_obj <- ModelTrainAnalysis(
object       = train_obj_test,
methods      = c("glm", "rf", "gbm"),
control      = list(method = "repeatedcv", number = 5, repeats = 1),
save_plots   = TRUE,
save_dir     = ".",
seed         = 123
)
PlotConfusionMatrix(model_obj, model_name = names(train_obj@train.models)[1], save_plot = FALSE)
}
}
