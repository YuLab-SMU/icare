% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object_utils.R
\name{FilterByMeta}
\alias{FilterByMeta}
\title{Filter object samples by a metadata column}
\usage{
FilterByMeta(object, col, values = NULL, range = NULL, invert = FALSE)
}
\arguments{
\item{object}{A \code{Stat}, \code{Subtyping}, or \code{PrognosiX} object.}

\item{col}{Column name in \code{info.data} (or \code{clean.data} for
\code{Train_Model}).}

\item{values}{Character / factor vector of allowed values. Ignored when
\code{range} is supplied.}

\item{range}{Numeric vector of length 2 \code{c(min, max)} for numeric
columns (inclusive). \code{values} is ignored when \code{range} is set.}

\item{invert}{Logical. When \code{TRUE} the matching samples are
\emph{excluded} rather than kept. Default \code{FALSE}.}
}
\value{
An object of the same class containing only the filtered samples.
}
\description{
Keeps only samples whose value in a specified \code{info.data} column
matches a set of allowed values, or falls within a numeric range.
}
\examples{
\dontrun{
sub_obj <- FilterByMeta(stat_obj_test, col = "SWAB", values = "0")
}
}
