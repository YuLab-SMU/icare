% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyping_08_compare.R
\name{plot_clustering_comparison}
\alias{plot_clustering_comparison}
\title{Plot Comparison Heatmap of Clustering Results}
\usage{
plot_clustering_comparison(
  object,
  methods = c("cluster_kmeans", "cluster_lpa", "cluster_nmf"),
  save_dir = NULL,
  width = 5,
  height = 4.5,
  base_size = 13,
  ...
)
}
\arguments{
\item{object}{A \code{Subtyping} S4 object containing clustering results
in the \code{info.data} slot.}

\item{methods}{Character vector of column names in \code{object@info.data}
that contain clustering assignments. Default is
\code{c("cluster_kmeans", "cluster_lpa", "cluster_nmf")}.}

\item{save_dir}{Character string specifying the directory to save the plot.
If \code{NULL}, the plot is not saved. Default is \code{NULL}.}

\item{width}{Numeric. Width of the saved PDF in inches. Default is \code{5}.}

\item{height}{Numeric. Height of the saved PDF in inches. Default is \code{4.5}.}

\item{base_size}{Numeric. Base font size for the heatmap. Default is \code{13}.}

\item{...}{Additional arguments passed to \code{\link[pheatmap]{pheatmap}}.}
}
\value{
Invisibly returns the \code{pheatmap} object. The heatmap is
  drawn on the current graphics device and optionally saved to PDF.
}
\description{
Generates a heatmap of Adjusted Rand Index (ARI) values comparing multiple
clustering methods. The heatmap displays pairwise similarity between
clustering solutions, with numerical values overlaid on each cell.
}
\examples{
\dontrun{
# Assuming 'obj' is a Subtyping object with multiple clustering results
plot_clustering_comparison(obj, save_dir = "./results")

# Customize with additional pheatmap arguments
plot_clustering_comparison(obj,
  methods = c("cluster_kmeans", "cluster_lpa"),
  save_dir = "./output",
  width = 6,
  height = 5,
  fontsize = 12
)
}
}
